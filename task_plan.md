# 多角色独立登录改造计划

## 目标
- 支持管理员、教师、学生在同一浏览器不同标签页同时登录
- 不同角色使用独立 token key 存储，互不覆盖
- 刷新页面后按当前路由自动恢复对应角色登录态

## 阶段
- [in_progress] 盘点当前 token 存储、路由守卫、请求拦截和登出入口
- [pending] 实现按角色前缀隔离的 token 工具
- [pending] 接入 store、登录、登出、路由守卫和请求拦截
- [pending] 诊断检查与构建验证

## 关键决策
- 使用 `admin_token`、`teacher_token`、`student_token` 三个 localStorage key
- 根据当前路径前缀 `/admin`、`/teacher`、`/student` 推导当前角色作用域
- 登录时根据返回角色写入对应 key；登出时只清当前作用域 key

## 风险
- 公共页面 `/login`、`/register`、`/` 没有固定角色前缀，需要显式选择或推导登录后目标角色
- request 拦截器需要根据当前路径实时取 token，不能继续只读单一 store.token

## 2026-05-05 班级管理查看学生功能

### 目标
- 在管理员 `班级管理` 页面新增 `查看学生` 操作按钮
- 点击后打开学生列表弹窗，展示当前班级下学生信息
- 学生列表支持独立分页，关闭后不影响班级列表当前分页和筛选状态

### 阶段
- [in_progress] 盘点班级管理页、用户分页接口和现有学生字段
- [pending] 在班级管理页实现查看学生按钮、弹窗表格和独立分页
- [pending] 诊断并验证前后端联调结果

### 关键决策
- 优先复用现有管理员用户分页接口 `/user/list`，通过 `role=student + classId` 查询班级学生
- 学生展示使用弹窗而非跳转新页面，确保关闭后自然返回当前班级管理上下文
- 学生状态在列表中展示为 `已激活/未激活`，加入时间使用现有 `createdAt` 字段承载

## 2026-05-05 班主任更换与取消

### 目标
- 修复班级管理中班主任重复绑定与静默覆盖问题
- 支持在编辑班级时更换班主任，并要求用户二次确认
- 支持在编辑班级时取消班主任，取消后教师端班主任权限同步失效

### 阶段
- [completed] 盘点 `ClassManage.vue`、`ClazzDTO`、`ClazzServiceImpl` 与教师端班主任权限判断
- [completed] 实现前端更换 / 取消班主任确认流程
- [completed] 实现后端允许置空、替换确认与班级-教师关系同步逻辑
- [completed] 完成诊断检查、前端构建与后端编译验证
